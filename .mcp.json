{
  "mcpServers": {
    "TestSprite": {
      "command": "cmd",
      "args": ["/c", "npx", "@testsprite/testsprite-mcp@latest"],
      "env": {
        "API_KEY": "${TESTSPRITE_API_KEY}"
      }
    }
  }
}
